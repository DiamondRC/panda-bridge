# Build output 
/build/
/out/
/cmake-build-*/

# Insource CMake fallback
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Testing/

# Preset overrides
CMakeUserPresets.json

# Compiled artifacts
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out
*.gch
*.pch

# Tooling caches / generated
/.cache/
.ccache/
/compile_commands.json

# Debug / sanitizer / profiling output
core
core.*
vgcore.*
*.gcda
*.gcno
*.profraw
perf.data
perf.data.old
callgrind.out.*
massif.out.*

# Editors / OS
.vscode/
.vscode-server/
.idea/
*.swp
*~
.DS_Store

# Stray home-dir artifact guard
.gnupg/
.ssh/
.config/
.dotnet/
.local/
.gitconfig

# Misc
notes/

# Kernal Build
kernel-build

# Boot deps
# (toolchain + source tarballs extracted from the kernel container)
boot-deps

# l2_lockdown kbuild artifacts
kernel/l2_lockdown/*.ko
kernel/l2_lockdown/*.mod*
kernel/l2_lockdown/*.o
kernel/l2_lockdown/.*.cmd
kernel/l2_lockdown/modules.order
kernel/l2_lockdown/Module.symvers
# Yocto / kas build tree (meta-custom builds here via ./kas.yml)
/sources/
/meta-panda/
/kas-container
/boot-pandabox/
/boot-pandabox.tar.gz

# LQR FPGA bitstream
meta-custom/recipes-panda/panda-fpga-lqr/files/panda_top.bin

# LQR boot.bin (FSBL + bitstream + u-boot): large, regenerated per synth. Local
# for bring-up (panda-fpga-boot bbappend reads it via file://); not committed.
meta-custom/recipes-panda/panda-fpga/files/boot/boot.bin
